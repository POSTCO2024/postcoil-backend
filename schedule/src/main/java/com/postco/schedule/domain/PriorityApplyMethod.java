package com.postco.schedule.domain;

public enum PriorityApplyMethod {
    DESC_GOALWIDTH,
    GROUPING_BY_GOALWIDTH,
    ASC_THICKNESS,
    APPLY_SIN,
    GROUPING_BY_THICKNESS,
    GROUPING_BY_TEMP,
    GROUPING_BY_NAME

}
